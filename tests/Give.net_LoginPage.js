import { Selector } from 'testcafe'

fixture `Give.net LoginTest`
        .page `https://knox/my.give.net/`
        
        /*Testcafe Actions
        Double clicks a webpage element
        t.doubleClick( selector [, options])
        selector identifies the webpage element being double clicked
        options are set of options that provide additional parameters for the action


        */
  test('LoginTest', async t =>{
      
      await t
      

  })      